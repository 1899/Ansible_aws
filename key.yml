---

# AWS Credentials

aws_access_key: "REDACTED"
aws_secret_key: "REDACTED"
region: "us-east-1"

# VPC 

cidr_ip: "10.20.0.0/16"
site_name: "YM"
app_name: "Test"
env: "POC"
cstc: "India"
email: "me@yesudasmallipeddi.online"

pusub1: "10.20.4.0/24"
pusub2: "10.20.6.0/24"
prsub1: "10.20.5.0/24"
prsub2: "10.20.7.0/24"


putag1:
  Name: "Public subnet 1"
  Environment: "{{ env }}"
  Costcenter: "{{ cstc }}"
  consumer: "{{ email }}"
putag2:
  Name: "Public subnet 2"
  Environment: "{{ env }}"
  Costcenter: "{{ cstc }}"
  consumer: "{{ email }}"
prtag1:
  Name: "Private Subnet 1"
  Environment: "{{ env }}"
  Costcenter: "{{ cstc }}"
  consumer: "{{ email }}"
prtag2:
  Name: "Private Subnet 2"
  Environment: "{{ env }}"
  Costcenter: "{{ cstc }}"
  consumer: "{{ email }}"

# Ec2 Details
ec2_key_pair: "ansibledemo"
